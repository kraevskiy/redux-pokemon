export enum ActionNavTypes {
  show_nav = "SHOW_NAV",
  hide_nav = "HIDE_NAV"
}

export enum ActionPokemonTypes {
  fetch = "FETCH_POKEMON",
  load = "LOAD"
}

export enum ActionPokemonImageTypes {
  fetch = "FETCH_IMAGES"
}


